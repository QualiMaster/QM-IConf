project Infrastructure {
	
	import Hardware;
	import ReconfigurableHardware;
	import Families;
	import Observables;
	import Adaptivity;
	import Pipelines;
   
	//refTo(Pipeline) pipeline; // use this as soon as possible -> reasoner Roman
	Pipeline pipeline; 
}